// rndc client configuration (used by the healthcheck and for manual `rndc` commands via
// `docker compose exec bind9 rndc -c /etc/bind/rndc.conf <command>`).
include "/run/secrets/bind_rndc_key";

options {
    default-key "rndc-key";
    default-server 127.0.0.1;
    default-port 953;
};
