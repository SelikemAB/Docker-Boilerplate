# Optional: publish nginx directly (without Traefik), e.g. for local testing.
# Usage: docker compose -f compose.yaml -f compose.expose-ports.yaml up -d
services:
  nginx:
    ports:
      - "${BIND_ADDRESS:-127.0.0.1}:${NGINX_HTTP_PORT:-8080}:8080"
